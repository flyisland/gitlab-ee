# frozen_string_literal: true

require 'spec_helper'

RSpec.describe Geo::<%= replicator_class_name %>, feature_category: :geo_replication do
  let(:model_record) { build(:<%= file_name %>) }

  include_examples 'a blob replicator'
end
