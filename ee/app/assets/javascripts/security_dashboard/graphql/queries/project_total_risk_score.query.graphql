# @feature_category: vulnerability_management
query projectTotalRiskScore($fullPath: ID!) {
  project(fullPath: $fullPath) {
    id
    securityMetrics {
      riskScore {
        score
      }
    }
  }
}
