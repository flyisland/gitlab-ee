# @feature_category: consumables_cost_management
query getCreditCaps($namespacePath: ID) {
  subscriptionUsage(namespacePath: $namespacePath) {
    budgetCaps {
      flatUserCap
      flatUserCapEnabled
    }
  }
}
