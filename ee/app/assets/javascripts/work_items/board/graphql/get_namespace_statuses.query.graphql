# @feature_category: portfolio_management
query getBoardNamespaceStatusesEE($fullPath: ID!) {
  namespace(fullPath: $fullPath) {
    id
    rootNamespace {
      id
      statuses {
        nodes {
          id
          name
          iconName
          color
          category
        }
      }
    }
  }
}
