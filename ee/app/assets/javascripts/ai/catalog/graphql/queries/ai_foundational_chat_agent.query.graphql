# @feature_category: workflow_catalog

query aiFoundationalChatAgent($reference: String!) {
  aiFoundationalChatAgent(reference: $reference) {
    id
    name
    description
    reference
    referenceWithVersion
    version
    avatarUrl
    systemPrompt
    visibility
    tools {
      id
      title
      description
    }
  }
}
