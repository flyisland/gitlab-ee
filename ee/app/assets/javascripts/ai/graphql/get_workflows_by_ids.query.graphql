# @feature_category: duo_agent_platform

query getWorkflowsByIds($ids: [AiDuoWorkflowsWorkflowID!]!) {
  duoWorkflowWorkflows(ids: $ids) {
    edges {
      node {
        id
        status
      }
    }
  }
}
