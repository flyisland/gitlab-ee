# @feature_category: team_planning
mutation customFieldUnarchive($id: IssuablesCustomFieldID!) {
  customFieldUnarchive(input: { id: $id }) {
    customField {
      id
      name
      fieldType
    }
    errors
  }
}
