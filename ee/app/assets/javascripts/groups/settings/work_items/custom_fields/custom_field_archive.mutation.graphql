# @feature_category: team_planning
mutation customFieldArchive($id: IssuablesCustomFieldID!) {
  customFieldArchive(input: { id: $id }) {
    customField {
      id
      name
      fieldType
    }
    errors
  }
}
