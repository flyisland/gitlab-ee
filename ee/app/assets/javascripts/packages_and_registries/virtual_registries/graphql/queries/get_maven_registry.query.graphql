# @feature_category: virtual_registry

query getMavenVirtualRegistry($id: VirtualRegistriesPackagesMavenRegistryID!) {
  registry: virtualRegistriesPackagesMavenRegistry(id: $id) {
    id
    name
    description
  }
}
