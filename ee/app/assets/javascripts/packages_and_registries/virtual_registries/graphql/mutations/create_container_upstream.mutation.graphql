# @feature_category: virtual_registry

mutation containerUpstreamCreate(
  $id: VirtualRegistriesContainerRegistryID!
  $name: String!
  $url: String!
  $description: String
  $username: String
  $password: String
  $cacheValidityHours: Int
) {
  createUpstream: containerUpstreamCreate(
    input: {
      id: $id
      name: $name
      url: $url
      description: $description
      username: $username
      password: $password
      cacheValidityHours: $cacheValidityHours
    }
  ) {
    upstream {
      id
      name
      url
      description
      cacheValidityHours
    }
    errors
  }
}
