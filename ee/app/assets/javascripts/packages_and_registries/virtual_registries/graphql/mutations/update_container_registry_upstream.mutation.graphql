# @feature_category: virtual_registry

mutation updateContainerRegistryUpstream(
  $id: VirtualRegistriesContainerRegistryUpstreamID!
  $position: Int!
) {
  update: containerVirtualRegistryUpstreamUpdate(input: { id: $id, position: $position }) {
    errors
  }
}
