# @feature_category: secrets_management
mutation startInstanceSecretsManagerTrial {
  secretsManagerInstanceStartTrial(input: {}) {
    entitlement {
      state
      blockedReason
      trialStartedAt
      trialExpiresAt
      creditsRemaining
      creditsTotal
      onDemandEnabled
      offlineLicense
    }
    errors
  }
}
