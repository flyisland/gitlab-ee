# @feature_category: continuous_delivery

query cdApplication($id: CdApplicationID!) {
  organization {
    id
    cdApplication(id: $id) {
      id
      name
      description
      health
      lastDeployedAt
      services {
        count
      }
      environments {
        count
      }
    }
  }
}
