# @feature_category: continuous_delivery

mutation cdVersionSetCreate($input: CdVersionSetCreateInput!) {
  cdVersionSetCreate(input: $input) {
    versionSet {
      id
      name
    }
    errors
  }
}
