# @feature_category: continuous_delivery

mutation cdEnvironmentCreate($input: CdEnvironmentCreateInput!) {
  cdEnvironmentCreate(input: $input) {
    environment {
      id
      name
      tier
    }
    errors
  }
}
