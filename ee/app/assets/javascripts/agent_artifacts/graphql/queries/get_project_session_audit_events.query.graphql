# @feature_category: ai_agents
query getProjectSessionAuditEvents(
  $projectFullPath: ID!
  $workflowId: AiDuoWorkflowsWorkflowID!
  $after: String
  $before: String
  $first: Int
  $last: Int
) {
  project(fullPath: $projectFullPath) {
    id
    duoWorkflowSessionArtifacts(workflowId: $workflowId) {
      nodes {
        id
        auditEvents(after: $after, before: $before, first: $first, last: $last) {
          nodes {
            id
            eventName
            createdAt
            details
            ipAddress
            workflowId
            author {
              id
              name
              username
              avatarUrl
              webPath
            }
          }
          pageInfo {
            hasNextPage
            hasPreviousPage
            startCursor
            endCursor
          }
        }
      }
    }
  }
}
