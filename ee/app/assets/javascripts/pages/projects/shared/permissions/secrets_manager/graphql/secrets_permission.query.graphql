# @feature_category: secrets_management

query secretPermissions($fullPath: ID!) {
  secretsPermissions: projectSecretsPermissions(projectPath: $fullPath) {
    nodes {
      expiredAt
      grantedBy {
        id
        username
        name
        avatarUrl
        webUrl
      }
      actions
      principal {
        id
        type
        userRoleId
        user {
          id
          username
          name
          avatarUrl
          webUrl
        }
        group {
          id
          name
          avatarUrl
          webUrl
        }
      }
      project {
        id
      }
    }
  }
}
