# @feature_category: secrets_management

mutation unenrollNamespaceSecretsManager($fullPath: ID!) {
  namespaceSecretsManagerUnenroll(input: { namespacePath: $fullPath }) {
    enrollment {
      namespace {
        id
      }
    }
    errors
  }
}
