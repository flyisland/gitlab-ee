# @feature_category: pipeline_composition

query getCatalogResourceComponentNames($fullPath: ID!) {
  ciCatalogResource(fullPath: $fullPath) {
    id
    webPath
    versions(first: 1) {
      nodes {
        id
        components {
          nodes {
            id
            name
          }
        }
      }
    }
  }
}
