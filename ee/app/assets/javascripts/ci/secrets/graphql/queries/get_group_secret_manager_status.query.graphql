# @feature_category: secrets_management

query getGroupSecretManagerStatus($fullPath: ID!) {
  secretsManager: groupSecretsManager(groupPath: $fullPath) {
    entity: group {
      id
      archived
      markedForDeletion
    }
    readOnly @gl_introduced(version: "19.4.0")
    status
  }
}
