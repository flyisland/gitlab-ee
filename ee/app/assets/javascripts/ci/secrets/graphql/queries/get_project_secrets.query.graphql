# @feature_category: secrets_management
query getProjectSecrets($fullPath: ID!) {
  secretsList: projectSecrets(projectPath: $fullPath) {
    edges {
      node {
        branch
        createdAt
        description
        environment
        metadataVersion
        name
        project {
          id
        }
        rotationInfo {
          rotationIntervalDays
          status
        }
        status
      }
    }
  }
}
