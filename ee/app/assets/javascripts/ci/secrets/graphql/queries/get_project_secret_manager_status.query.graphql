# @feature_category: secrets_management
query getProjectSecretManagerStatus($fullPath: ID!) {
  secretsManager: projectSecretsManager(projectPath: $fullPath) {
    entity: project {
      id
      archived
      markedForDeletion
    }
    status
  }
}
