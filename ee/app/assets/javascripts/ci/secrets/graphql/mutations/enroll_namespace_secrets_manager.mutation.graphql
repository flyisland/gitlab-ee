# @feature_category: secrets_management

mutation enrollNamespaceSecretsManager($fullPath: ID!) {
  namespaceSecretsManagerEnroll(input: { namespacePath: $fullPath }) {
    enrollment {
      namespace {
        id
      }
    }
    errors
  }
}
