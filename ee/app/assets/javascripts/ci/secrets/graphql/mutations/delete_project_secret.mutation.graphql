# @feature_category: secrets_management
mutation deleteProjectSecret($fullPath: ID!, $name: String!) {
  deleteSecret: projectSecretDelete(input: { projectPath: $fullPath, name: $name }) {
    errors
  }
}
