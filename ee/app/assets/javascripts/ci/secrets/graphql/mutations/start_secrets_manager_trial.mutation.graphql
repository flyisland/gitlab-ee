# @feature_category: secrets_management
mutation startSecretsManagerTrial($groupPath: ID!) {
  secretsManagerStartTrial(input: { groupPath: $groupPath }) {
    entitlement {
      state
      blockedReason
      trialStartedAt
      trialExpiresAt
      creditsRemaining
      creditsTotal
      onDemandEnabled
    }
    errors
  }
}
