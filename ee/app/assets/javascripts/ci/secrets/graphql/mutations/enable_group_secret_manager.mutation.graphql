# @feature_category: secrets_management
mutation enableGroupSecretManager($fullPath: ID!) {
  groupSecretsManagerInitialize(input: { groupPath: $fullPath }) {
    errors
    groupSecretsManager {
      status
    }
  }
}
