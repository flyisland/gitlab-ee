# @feature_category: secrets_management
mutation disableSecretManager($fullPath: ID!) {
  projectSecretsManagerDeprovision(input: { projectPath: $fullPath }) {
    errors
    projectSecretsManager {
      status
    }
  }
}
