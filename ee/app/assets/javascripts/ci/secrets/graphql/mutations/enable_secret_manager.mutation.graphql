# @feature_category: secrets_management
mutation enableSecretManager($fullPath: ID!) {
  projectSecretsManagerInitialize(input: { projectPath: $fullPath }) {
    errors
    projectSecretsManager {
      status
    }
  }
}
