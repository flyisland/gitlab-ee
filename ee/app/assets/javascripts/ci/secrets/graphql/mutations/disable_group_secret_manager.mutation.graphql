# @feature_category: secrets_management
mutation disableGroupSecretManager($fullPath: ID!) {
  groupSecretsManagerDeprovision(input: { groupPath: $fullPath }) {
    errors
    groupSecretsManager {
      status
    }
  }
}
