# @feature_category: secrets_management
mutation deleteGroupSecret($fullPath: ID!, $name: String!) {
  deleteSecret: groupSecretDelete(input: { groupPath: $fullPath, name: $name }) {
    errors
  }
}
