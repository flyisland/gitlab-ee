{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Schema for the GitLab secret detection false positive detection scan profile configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "severity_level": {
      "description": "Minimum vulnerability severity that triggers false positive detection. Findings below this threshold are skipped.",
      "type": "string",
      "enum": [
        "info",
        "unknown",
        "low",
        "medium",
        "high",
        "critical"
      ]
    },
    "run_mode": {
      "description": "Whether false positive detection runs automatically or only when triggered by a user.",
      "type": "string",
      "enum": [
        "manual",
        "auto"
      ]
    }
  }
}
