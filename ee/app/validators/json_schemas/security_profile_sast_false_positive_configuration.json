{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Schema for the GitLab SAST false positive detection scan profile configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "severity_level": {
      "description": "Minimum vulnerability severity that triggers false positive detection. Findings below this threshold are skipped.",
      "type": "string",
      "enum": [
        "info",
        "unknown",
        "low",
        "medium",
        "high",
        "critical"
      ]
    },
    "cwe_classes": {
      "description": "CWE identifiers to restrict false positive detection to.",
      "type": "array",
      "maxItems": 50,
      "items": {
        "type": "string",
        "pattern": "^CWE-\\d{1,5}$"
      }
    },
    "run_mode": {
      "description": "Whether false positive detection runs automatically or only when triggered by a user.",
      "type": "string",
      "enum": [
        "manual",
        "auto"
      ]
    }
  }
}
