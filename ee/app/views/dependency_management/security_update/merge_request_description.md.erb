## Security Dependency Update

This merge request was automatically generated by **GitLab Dependency Management**
to remediate a known security vulnerability by updating affected dependencies
to patched versions.

---

## Dependency Changes

| Package | Previous Version | New Version |
|---------|-----------------|-------------|
<% dependencies.each do |dep| -%>
| <%= code_span(dep.name) %> | <%= code_span(dep.previous_version) %> | <%= code_span(dep.version) %> |
<% end -%>

---

## Vulnerability Details

This merge request addresses [vulnerability #<%= vulnerability.id %>](<%= vulnerability_url %>).
View the link for full details, including severity and CVE information.

---

## Files Changed

<% updated_files.each do |file| -%>
- <%= code_span(file.path) %>
<% end -%>

---

## What to do next

1. **Review** the dependency changes and ensure they don't introduce breaking changes
2. **Verify** that CI/CD pipelines pass successfully
3. **Merge** this MR to apply the security fix

> This MR was created by the GitLab Dependency Management service account.
