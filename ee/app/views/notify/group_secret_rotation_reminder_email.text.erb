<%= s_("SecretRotation|Hi %{name}!") % { name: sanitize_name(@user.name) } %>

<%= s_("SecretRotation|Your secret '%{secret_name}' in group %{group_name} requires rotation.") % { secret_name: @secret_name, group_name: @group.name } %>

<%= s_("SecretRotation|Please change the secret value to maintain security.") %>

<%= s_("SecretRotation|You can update the secret at: %{target_url}!") % { target_url: @target_url } %>
