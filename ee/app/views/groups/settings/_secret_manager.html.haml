- return unless secrets_manager_available_for_group?(@group) || allow_secrets_manager_namespace_enrollment?(@group)
- hide_inline_text = local_assigns.fetch(:hide_inline_text, false)

.js-group-secrets-manager-settings{ data: namespace_enrollment_data(@group, current_user).merge(hide_inline_text: hide_inline_text.to_s) }
