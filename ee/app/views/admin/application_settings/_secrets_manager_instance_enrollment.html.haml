- return unless License.feature_available?(:native_secrets_management) && allow_secrets_manager_instance_enrollment?

- secrets_manager_heading = s_('SecretsManager|GitLab Secrets Manager')
= render ::Layouts::SettingsBlockComponent.new(secrets_manager_heading,
  id: 'js-secrets-manager-instance-enrollment-settings',
  testid: 'admin-secrets-manager-instance-enrollment-settings',
  expanded: expanded_by_default?) do |c|
  - c.with_heading do
    %span.gl-inline-flex.gl-items-center.gl-gap-3
      = secrets_manager_heading
      = render Pajamas::BadgeComponent.new(_('New'), variant: :info)
  - c.with_description do
    = s_('SecretsManager|Enable GitLab Secrets Manager for all groups and projects in this instance.')
    = link_to(_('Learn more.'), help_page_path('ci/secrets/secrets_manager/_index.md'), target: '_blank', rel: 'noopener noreferrer')
  - c.with_body do
    #js-secrets-manager-instance-enrollment
