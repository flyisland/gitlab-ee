---
description: When a user requests a code explanation with the GitLab Duo Chat /explain command
internal_events: true
status: active
# The action name matches the feature's canonical id (unit primitive and
# register_feature name) so usage traces to one name across ai_usage_events,
# the GraphQL enum, and the generated ExplainCodeUserMetrics type.
action: explain_code
classification: duo
identifiers:
- project
- namespace
- user
product_group: duo_chat
product_categories:
- duo_chat
milestone: '19.5'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/246385
tiers:
- premium
- ultimate
