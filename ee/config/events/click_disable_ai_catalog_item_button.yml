---
description: AI Catalog item Disable button click
internal_events: true
status: active
action: click_disable_ai_catalog_item_button
identifiers:
  - project
  - namespace
  - user
additional_properties:
  label:
    description: Type of AI Catalog item
  page:
    description: Catalog page of origin (List / Show item)
  origin:
    description: Catalog area of origin (Explore / Project / Group)
  item_type:
    description: Item type. One of custom_flow, custom_agent, custom_external_agent,
      foundational_flow, foundational_agent, foundational_external_agent.
  item_version:
    description: Semantic version of the item (e.g. "1.0.0").
  item_schema_version:
    description: Flow Schema version (e.g. "v1").
  flow_name:
    description: Foundational flow name.
  custom_item_id:
    description: Custom item ID.
  tools:
    description: Comma-separated built-in tool names used by the item. Only present
      when disabled from the item show page.
  mcp_tools:
    description: Comma-separated MCP tool names used by the item. Only present when
      disabled from the item show page.
  mcp_servers:
    description: Comma-separated MCP server IDs used by the item. Only present when
      disabled from the item show page.
  components:
    description: Comma-separated component types defined in the flow. Only present
      when disabled from the item show page.
product_group: workflow_catalog
product_categories:
  - workflow_catalog
milestone: '18.9'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/216766
tiers:
  - premium
  - ultimate
classification: duo
